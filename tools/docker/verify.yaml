name: ai-skills

services:
  verify:
    image: ai-skills-checks
    build:
      context: ../..
      dockerfile: tools/docker/Dockerfile
    working_dir: /workspace
    command: ["sh", "tools/scripts/verify.sh"]
    volumes:
      - ../..:/workspace:ro
